- name: secondloop
  hosts: linuxserver
  become: yes
  tasks:
   - name: chudam
     ansible.builtin.dnf:
      name: " {{ item }} "
      state: started
     loop:
     - mysql
     - nginx
     - git
